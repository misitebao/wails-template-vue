{
  "name": "{{.ProjectName}}",
  "private": true,
  "version": "0.1.0",
  "description": "A Wails Template",
  "main": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "{{.AuthorName}}",
  "license": "MIT",
  "workspaces": ["packages/*"],
  "engines": {
    "node": ">=15.0.0",
    "npm": ">=7.0.0"
  }
}
